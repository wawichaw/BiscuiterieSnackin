# Configuration Frontend - Copiez ce fichier en .env et remplissez les valeurs

# Stripe - Clé PUBLIQUE uniquement (peut être exposée)
# Obtenez votre clé publique depuis: https://dashboard.stripe.com/apikeys
VITE_STRIPE_PUBLIC_KEY=pk_test_votre_cle_publique_stripe

# URL de l'API backend
VITE_API_BASE_URL=http://localhost:5000/api

# Sentry - monitoring des erreurs frontend (optionnel, DSN public)
# VITE_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# Google OAuth - Client ID (obtenu depuis Google Cloud Console)
# VITE_GOOGLE_CLIENT_ID=ton-client-id-google.apps.googleusercontent.com

# ⚠️ IMPORTANT : Ne JAMAIS mettre de clés secrètes ici !
# Les clés secrètes (STRIPE_SECRET_KEY, SMTP_PASSWORD, etc.) doivent être uniquement dans backend/.env
