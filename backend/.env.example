# Configuration Backend - Copiez ce fichier en .env et remplissez les valeurs

# Base de données MongoDB
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/snackin?retryWrites=true&w=majority

# JWT - GÉNÉRER UN SECRET FORT (minimum 32 caractères)
# Utilisez: openssl rand -base64 32
JWT_SECRET=votre_secret_jwt_tres_securise_32_caracteres_minimum
JWT_EXPIRE=24h

# Serveur
PORT=5000
NODE_ENV=development

# Frontend URL (pour CORS)
FRONTEND_URL=http://localhost:3000

# Stripe - Clés SECRÈTES (ne jamais exposer dans le frontend)
# Obtenez vos clés depuis: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_votre_cle_secrete_stripe

# Envoi d'emails - UNE des options (la première trouvée est utilisée) :
# Option 1 - SendGrid (le plus simple sur Render, pas de DNS) :
# 1. Créez un compte sur https://sendgrid.com
# 2. Settings > Sender Authentication > Verify a Single Sender : ajoutez snackin.mtl@gmail.com (vous recevrez un lien à cliquer)
# 3. Create API Key (Mail Send - Full Access), copiez la clé
# 4. Sur Render : SENDGRID_API_KEY=votre_cle  et  SENDGRID_FROM=Snackin' <snackin.mtl@gmail.com>
SENDGRID_API_KEY=
SENDGRID_FROM=Snackin' <snackin.mtl@gmail.com>

# Option 2 - Resend (nécessite vérification de domaine pour envoyer à tous)
# RESEND_API_KEY=re_xxx
# RESEND_FROM=Snackin' <noreply@votredomaine.com>

# Option 3 - SMTP (Gmail, etc.) - bloqué sur Render (timeout), OK en local
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=votre_email@gmail.com
SMTP_PASSWORD=votre_app_password_16_caracteres

# Monitoring des erreurs (optionnel) - https://sentry.io
# Créez un projet Node/Express et copiez le DSN ici pour activer Sentry
# SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# Google OAuth (optionnel) - https://console.cloud.google.com
# Créez un projet OAuth 2.0 et copiez le Client ID ici
# GOOGLE_CLIENT_ID=ton-client-id-google.apps.googleusercontent.com
